-# nested_start
.col-md-6
  %div{class: 'panel panel-'+ (i.kind_of?(Integer) ? 'success' : 'info')}
    .panel-heading
      Роль
      = i
      %span.toright
        -# Кнопка для удаления связи роль-пользователь. См. role_users_helper
        = link_to_remove_collection(fr)
      .clearfix
    .panel-body
      .row
        .col-md-6
          .form-group
            = fr.label :collection_id
            = fr.select :id, Collection.all.map{|x| [x.colname, x.id]}, {}, {class: 'form-control'}

-# nested_finish

